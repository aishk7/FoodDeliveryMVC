@model IEnumerable<FoodItem>

<div class="row" style="margin:40px">
    @foreach (var item in Model)
    {
        <partial name="_specialItems" model="item" />
        @*<div class="col-sm-6">
            <div class="card-deck">
                <div class="card shadow">
                    <img class="card-img-top img-thumbnail" src="@item.ImgURL" style="width:100%; height:300px"/>
                    <div class="card-body">
                        <h3 class="pull-right">&#8377 @item.Price</h3>
                        <h4 class="card-title">@item.ItemName</h4>
                        <p class="card-text">@item.Details</p>
                        <a class="lnk btn btn-success btn-block">Add to Cart</a>
                    </div>
                </div>
            </div>

        </div>*@
    }
</div>
